# .github/workflows/terraform.yml
name: 'Terraform Infrastructure Deployment'

on:
  push:
    branches:
      - main
    paths:
      - 'terraform/**' # Trigger only on changes to Terraform code
  pull_request:
    branches:
      - main
    paths:
      - 'terraform/**'

env:
  # Reference the GitHub Secret you created
  AWS_REGION: ${{ secrets.AWS_REGION }} 
  TF_VERSION: 1.6.0 # Specify a stable version

jobs:
  terraform:
    name: 'Terraform'
    runs-on: ubuntu-latest
    
    # CRITICAL FIX 1: Set the working directory to find your .tf files
    defaults:
      run:
        working-directory: ./terraform
        shell: bash

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # CRITICAL FIX 2: Replaces Terraform Cloud token step with AWS credentials
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      # FIX 3: Update to latest stable setup-terraform action (v3)
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          # REMOVED: cli_config_credentials_token is no longer needed

      - name: Terraform Init
        id: init
        run: terraform init

      - name: Terraform Format Check
        run: terraform fmt -check

      - name: Terraform Plan
        id: plan
        # We run plan on all PRs to main
        if: github.event_name == 'pull_request' 
        run: terraform plan -no-color -input=false

      # CRITICAL FIX 4: Corrected conditional for 'main' branch apply
      - name: Terraform Apply
        # Runs ONLY when the main branch is pushed (e.g., after a PR merge)
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: terraform apply -auto-approve -input=false
